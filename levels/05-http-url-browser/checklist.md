# 第 05 关自检清单（`checklist.md`）

## 路径与服务

1. [ ] 我已经解压本关练习包（或已从整仓进入本关 `starter`）。
2. [ ] 我能找到 `practice/starter/`（ZIP 内为 `level-05-http-url-browser-starter/`）。
3. [ ] 我能双击打开 **`START_HERE.html`**。
4. [ ] 我能在终端 **`cd`** 进入 **`starter`** 文件夹。

## HTTP 访问

5. [ ] 我能启动 **`python3 -m http.server 8005`**（若不可用，已尝试 `py -m http.server 8005` 或 `python -m http.server 8005`）。
6. [ ] 我能用浏览器访问 **`http://127.0.0.1:8005/`**（首页）。
7. [ ] 我能访问 **`http://127.0.0.1:8005/hello.html`**。
8. [ ] 我能访问 **`http://127.0.0.1:8005/data.json`**。
9. [ ] 我能访问 **`http://127.0.0.1:8005/not-exist.html`** 并看到 **404**（或浏览器显示的「未找到」等价提示）。

## 修改与总结

10. [ ] 我至少修改并保存了 **`index.html`** 或 **`data.json`**（或两者）。
11. [ ] 我已经填写 **`notes/task-note.txt`**。
12. [ ] 我能用一句话分别说明：**URL**、**请求**、**响应**、**状态码** 在本关练习里指什么。

## 建议提交物

1. 本地 HTTP 服务终端截图（可见监听命令与端口）。  
2. 浏览器访问 **`index.html`（通过 http 根路径）** 的截图。  
3. 浏览器访问 **`data.json`** 的截图。  
4. **404** 页面截图。  
5. **`notes/task-note.txt`** 内容截图。
