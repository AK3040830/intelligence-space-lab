为什么访问 not-exist.html 会看到 404？

本练习包里没有名为 not-exist.html 的文件。
当你用浏览器请求 http://127.0.0.1:8005/not-exist.html 时，
Python 的 http.server 会在当前目录下查找这个文件；
找不到资源时，HTTP 状态码通常为 404（Not Found），
表示「服务器收到了请求，但没有你要的那份内容」。

这与「地址写错」「还没启动 http.server」等情况不同；
本关只要求你先把「404 ≈ 要找的文件不存在」对上号。
